<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis-mapper">
	
	<!-- 결과를 VO와 DB의 컬럼이 다를때 사용하는 resultMap -->
	<!-- 
	<resultMap id="resultMember" type="Member">
		<id property="userId" column="USERID"/>
		<result property="password" column="password"/>
		<result property="userName" column="nickName"/>
		<result property="userEmail" column="USER_EMAIL"/>
	</resultMap>
	-->
	<!-- DAO ) sqlSession.select("mapper명", member); -->
	<!-- 
	<select id="selectMember" resultType="Member">
		SELECT*FROM MEMBER
		WHERE USERID = #{userId}
	</select> -->
	<!-- <insert id="insertMember" parameterType="Member"> INSERT INTO MEMBMER 
		VALUES (#{userId}, #{password}, ...) </insert> -->


</mapper>
 